<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Dart 분석</title>
        <link rel="stylesheet" href="/company-tables.css"/>
    </head>
    <body>
        <%- include('../common_html/main_title') %>
        <div class="main-body">
            <%- include('../common_html/menu') %>
            <div class="main-content">
                <div class="sub-title-wrap">
                    <span class="sub-title">공시검색</span>
                </div>
                <div class="main-tables-wrap">
                    <%- include('../common_html/company_table') %>
                    <div class="disclosure-detail">
                        <% if (disclosureData && disclosureData.status === "000") { %>
                            <h3>공시 목록</h3>
                            <table border="1">
                            <tr><th>접수일자</th><th>보고서명</th><th>접수번호</th></tr>
                            <% disclosureData.list.forEach(function(item) { %>
                                <tr>
                                <td><%= item.rcept_dt %></td>
                                <td><%= item.report_nm %></td>
                                <td>
                                    <a href="https://dart.fss.or.kr/dsaf001/main.do?rcpNo=<%= item.rcept_no %>" target="_blank">
                                    <%= item.rcept_no %>
                                    </a>
                                </td>
                                </tr>
                            <% }) %>
                            </table>
                        <% } else if (disclosureData) { %>
                            <p>공시 데이터 없음: <%= disclosureData.message %></p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>